Create database Clinic;

create table patients(id SERIAL PRIMARY KEY,fname varchar(255),lname varchar(255),age int,gender varchar(10),address varchar(255),pNo int);

create table medicines(id serial primary key,name varchar(255),price int);

select * from patients;
select * from medicines;

drop table patients;
drop table medicines;